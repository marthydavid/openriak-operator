# permissions for end users to view riakbuckets.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: agents-riak-operator-kubernetes-lifecycle
    app.kubernetes.io/managed-by: kustomize
  name: riakbucket-viewer-role
rules:
- apiGroups:
  - riak.openriak.io
  resources:
  - riakbuckets
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - riak.openriak.io
  resources:
  - riakbuckets/status
  verbs:
  - get
