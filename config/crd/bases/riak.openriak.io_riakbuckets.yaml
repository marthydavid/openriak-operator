---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: riakbuckets.riak.openriak.io
spec:
  group: riak.openriak.io
  names:
    kind: RiakBucket
    listKind: RiakBucketList
    plural: riakbuckets
    singular: riakbucket
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RiakBucket is the Schema for the riakbuckets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RiakBucketSpec defines the desired state of RiakBucket.
            properties:
              allowMulti:
                description: AllowMulti allows multiple values for same key (siblings).
                type: boolean
              bucketName:
                description: BucketName is the name of the bucket.
                minLength: 1
                type: string
              bucketType:
                description: BucketType is the bucket type (default is "default").
                type: string
              clusterName:
                description: ClusterName is the name of the RiakCluster this bucket
                  belongs to.
                minLength: 1
                type: string
              nVal:
                description: NVal is the number of replicas (n_val).
                format: int32
                minimum: 1
                type: integer
              properties:
                additionalProperties:
                  type: string
                description: Properties contains custom bucket properties as key-value
                  pairs.
                type: object
              replicationFactor:
                description: ReplicationFactor is the number of replicas.
                format: int32
                maximum: 999
                minimum: 1
                type: integer
            required:
            - bucketName
            - clusterName
            type: object
          status:
            description: RiakBucketStatus defines the observed state of RiakBucket.
            properties:
              created:
                description: Created indicates if the bucket was successfully created.
                type: boolean
              error:
                description: Error contains error details if creation failed.
                type: string
              lastUpdateTime:
                description: LastUpdateTime is when the bucket was last updated.
                format: date-time
                type: string
              phase:
                description: Phase is the current phase (Creating, Ready, Failed).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
