---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: riakclusters.riak.openriak.io
spec:
  group: riak.openriak.io
  names:
    kind: RiakCluster
    listKind: RiakClusterList
    plural: riakclusters
    singular: riakcluster
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RiakCluster is the Schema for the riakclusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RiakClusterSpec defines the desired state of RiakCluster.
            properties:
              image:
                description: |-
                  Image is the Riak Docker image to use (e.g., ghcr.io/marthydavid/riak:3.2.6).
                  When omitted, the operator's --riak-image flag value is used as the default.
                minLength: 1
                type: string
              imagePullPolicy:
                description: ImagePullPolicy defines how the image should be pulled.
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector for node affinity.
                type: object
              resources:
                description: Resources specifies compute resources for each Riak node.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              riakConfig:
                additionalProperties:
                  type: string
                description: RiakConfig is the riak.conf configuration for all nodes.
                type: object
              servicePort:
                description: ServicePort is the port for the Riak protocol service.
                format: int32
                maximum: 65535
                minimum: 1024
                type: integer
              size:
                description: Size is the number of nodes in the cluster.
                format: int32
                maximum: 999
                minimum: 1
                type: integer
              storageClassName:
                description: StorageClassName for persistent volumes.
                type: string
              storageSize:
                anyOf:
                - type: integer
                - type: string
                description: StorageSize is the size of storage for each Riak node
                  (e.g., 10Gi).
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              tls:
                description: TLS configuration for inter-node communication.
                properties:
                  certManager:
                    description: CertManager enables cert-manager integration.
                    properties:
                      issuerKind:
                        description: IssuerKind is the kind of issuer (Issuer or ClusterIssuer).
                        type: string
                      issuerName:
                        description: IssuerName is the cert-manager issuer to use.
                        type: string
                    type: object
                  enabled:
                    description: Enabled specifies if TLS should be used.
                    type: boolean
                type: object
            required:
            - size
            type: object
          status:
            description: RiakClusterStatus defines the observed state of RiakCluster.
            properties:
              conditions:
                description: Conditions represent the latest observed conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastUpdateTime:
                description: LastUpdateTime is the last time the cluster was updated.
                format: date-time
                type: string
              members:
                description: Members is a list of current cluster members.
                items:
                  description: RiakNodeMember represents a cluster member node.
                  properties:
                    health:
                      description: Health is the health status of the node.
                      type: string
                    name:
                      description: Name is the member name.
                      type: string
                    pod:
                      description: Pod is the Kubernetes Pod name.
                      type: string
                    ready:
                      description: Ready indicates if the node is ready.
                      type: boolean
                  type: object
                type: array
              phase:
                description: Phase is the current phase of the cluster (Creating,
                  Ready, Updating, Failed).
                type: string
              readyNodes:
                description: ReadyNodes is the number of ready nodes.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
